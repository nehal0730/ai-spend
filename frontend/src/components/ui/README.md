This folder is reserved for shadcn/ui generated primitives.

To initialize shadcn components (interactive), run:

```
npx shadcn-ui@latest init
```

After running the init command, the CLI will scaffold component primitives under this folder and update your Tailwind setup.

If you prefer manual setup, add accessible primitives (Button, Input, Modal, DataTable) here following the shadcn patterns.
