# ai-spend backend

This backend scaffold uses Node.js, Express, and TypeScript with production-grade middleware.

Quick start

```bash
cd backend
npm install
cp .env.example .env
npm run dev
```

Features
- Helmet, CORS, rate limiting
- Centralized error handling
- Request validation with Zod
- Logging (morgan + winston)
